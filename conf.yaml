#Nombre de spout
spout.count : 32
#Nombre de bolt splitter
splitter.count : 1
#Nombre de bolt counter
counter.count : 1

# Nombre de tache (généralement 1 tache = 1 component(spout, bolt))
spout.task : 32
splitter.task : 1
counter.task : 1

# Ne pas changer / nombre de bolt reporter
reporter.count : 1

# Topologie doubleur (=1 --> duplique la topologie si dessus sur tout les workers)
topology.doubler : 1

# Nombre de worker
# storm config overrides
topology.workers : 1
metric.count : 1
